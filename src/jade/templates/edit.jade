.tyto-edit
  h1 editing a todo <%= title %>
  p <%= board.id %>
  p <%= columns.length %>
  p <%= id %>
  |<% if (isNew) { %>
  button.tyto-edit__cancel Cancel
  |<% } else { %>
  a.tyto-edit__back(href!="#board/<%= board.id %>") Go back
  |<% } %>
